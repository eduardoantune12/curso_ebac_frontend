function multiplicar(num1: number, num2: number): number {
    return num1 * num2;
}

function dizOi(nome: string): string {
    return `Olá, ${nome} seja bem vindo!`;
}

console.log(multiplicar(7, 8));
console.log(dizOi("Eduardo"));
